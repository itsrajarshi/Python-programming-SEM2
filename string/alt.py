# Input the original string
text = input("Enter a string: ")

# Form a new string with every alternate character
alternate_chars = text[::2]

# Output the result
print("New string with alternate characters:", alternate_chars)